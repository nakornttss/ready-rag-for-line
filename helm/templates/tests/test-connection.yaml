apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ready-rag-for-line-chart.fullname" . }}-test-connection"
  labels:
    {{- include "ready-rag-for-line-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ready-rag-for-line-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
